import { Career } from "@/src/page-components";
import type { Metadata } from "next";

export const metadata: Metadata = {
   title: "Career",
   description: "app",
};

const CareerPage = () => <Career />;

export default CareerPage;
